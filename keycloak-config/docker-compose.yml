services:
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.5
    container_name: yandex-keycloak
    command:
      - start-dev
      - --http-port=9990
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HOSTNAME: localhost
    ports:
      - "9990:9990"