version: '3.8'
services:
  zookeeper:
    image: bitnami/zookeeper:latest
    environment:
      - ZOOKEEPER_CLIENT_PORT=2181
      - ZOOKEEPER_SERVER_ID=1
    ports:
      - "2181:2181"
    networks:
      - bank-network

networks:
  bank-network:
    driver: bridge